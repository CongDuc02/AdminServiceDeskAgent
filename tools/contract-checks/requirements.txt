# Phiên bản đã dùng khi chạy thật ở Phase 6 (2026-09-13) — không phải phiên bản ghi từ trí nhớ.
psycopg[binary]==3.3.5
langgraph-checkpoint-postgres==3.1.2
# Chỉ cần cho chế độ --local:
pgserver==0.1.4
